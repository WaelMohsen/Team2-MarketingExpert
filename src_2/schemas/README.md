# JSON Schemas

These schemas document machine-readable interchange outputs. The Pydantic models
under `src_2/contracts` are the executable source of truth. In a later increment,
the JSON Schema files should be generated from those models during CI to prevent
drift.
