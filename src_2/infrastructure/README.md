# Infrastructure Adapters

This area will contain concrete implementations for configuration loading, cycle
repositories, LLM providers, and artifact storage. Application code depends on
ports, allowing these technologies to change without changing business rules.
